# Whatbot configuration
SERVER_PORT=8080
MY_SERVER_API_KEY=
JINA_API_KEYS=
TAVILY_API_KEYS=

# Optional ngrok configuration.
# Keep the token out of source control. Set these in the runtime environment/.env.
NGROK_ENABLED=false
NGROK_DOMAIN=
NGROK_AUTHTOKEN=
